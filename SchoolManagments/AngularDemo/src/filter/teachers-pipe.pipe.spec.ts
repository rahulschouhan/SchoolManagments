import { TeachersPipePipe } from './teachers-pipe.pipe';

describe('TeachersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TeachersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
